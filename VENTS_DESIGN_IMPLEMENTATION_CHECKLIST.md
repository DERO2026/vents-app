# VENTS Design Implementation — Master Checklist

**Source of truth:** `/tmp/vents_design/stripped.html` (Claude Design export, ~67 marked screens A1-TB2) plus two follow-up design docs the user supplied directly in-conversation: `VENTS Ticket Security and Media Upload.dc.html` (ticket/QR states, check-in scanner states, event image upload/cropper, 6-step Create/Edit Event workflow) and `VENTS Redesign.dc.html`/`Canvas.dc.html` (logo assets, general design-system audit). Those two files are not in the repo — re-obtain from the user if a future session needs to re-read them directly rather than relying on this summary.

**Rule for every future session:** EXPORTED DESIGN → REAL REACT SCREEN → RENDER (via `qa-harness/`, the only way to render real components without live Supabase credentials in this sandbox) → DIRECT VISUAL COMPARISON → FIX → RENDER AGAIN → VERIFY. Never mark something MATCH because a component/function exists or the anatomy is merely similar — only because a rendered screenshot was actually compared against the export's actual markup/copy/layout. Never fabricate data or UI not backed by real Supabase schema/RLS. Preserve all backend/payment/wallet/booking/auth/security/RLS logic — UI-only changes unless a screen genuinely requires wiring existing functionality differently.

## Verified DONE this session (real screenshot comparison, not source-reading)

- A2 CountrySelectScreen, B1 HomeScreen (hero card + carousels — was hidden by thin harness fixture data, not missing UI), C1 EventDetailScreen (fixed: tab-strip/description order was wrong — now matches export), C3 Checkout, D1 MyTickets, D2 QRTicket (light backing), C6 PaymentSuccess (icon/QR styling), CS1/CS2 Creator Studio (fixed: revenue "Free"-for-zero bug), F1 Wallet (fixed: missing logo watermark), PD1-3 SettingsScreen incl. new PD2 Connected Accounts (real migration-backed), SV1 ServicesHomeScreen, CH3 Notifications, PMG1 ServiceProviderSetupScreen, PV3 ServiceProviderVerificationScreen, B3 FilterSheet (chip styling literally matches export's exact hex values), S2 PaymentFailedScreen, S5 expired payment request, S6 TicketRefundScreen, SA1/SA2 SalesAnalytics, PK1-3 pickers (bottom-sheet PickerSheet, inline PhoneInput/state dropdown).
- Real bug fixed independent of the mockup: `EventMap.tsx` was auto-opening an unstyled white Google InfoWindow popup on load, clashing with the dark theme — now only opens on marker tap.
- QA harness extended to ~35 routes and several real fixture/shim bugs fixed along the way (documented in commit history, not restated here since the user does not want harness-repair framed as redesign progress).

## NOT yet verified/implemented at real rendered-pixel rigor — this is the actual remaining scope, in the user's specified batch order

### Batch 1 — AUTH + ONBOARDING — ✅ DONE (real screenshot comparison against A1/A3/A4/A5/LG1/A2)
- A1 WelcomeScreen: rendered and compared — logo, headline, subtitle, 3-card rotated stack with EVENTS pill, Get Started + Log in link all match the export's layout. MATCH, no changes needed.
- LG1 Login: rendered and compared line-for-line against the export (logo, "Welcome back" + subtitle, Email-or-username field, Password field with Show, right-aligned Forgot password?, purple Log in CTA with glow, "New to VENTS? Create an account"). MATCH, no changes needed.
- A3 SignUpScreen: rendered and compared. Real form has 3 extra real fields the export doesn't show (photo upload, username, date of birth) — verified these are genuine, non-fabricated requirements: username backs the @handle used everywhere else in the app, DOB backs the existing 18+ event-gating logic already live elsewhere. Core export fields (Full name, Email, Phone, Password) are present in the same relative order. Left as-is — removing real required fields to look like the simpler export would violate data integrity, not fix a UI bug.
- A4 VerifyScreen (signup OTP): rendered via the real `pendingVerificationEmail` prop (jumps straight to the OTP UI, no live Supabase needed). Structure matches the export (Step 3 of 3, title, subtitle with email, code boxes, Didn't get it?/Resend row, Verify CTA, Change email address link). One real code digit-count difference confirmed CORRECT, not a bug: the export assumes 6 digits, but `EMAIL_OTP_LENGTH = 8` in `AuthScreen.tsx` is pinned to this Supabase project's actual `mailer_otp_length` setting (verified via the Management API per the code's own comment) — changing it to 6 would break real signups, since the email would keep sending 8-digit codes. Left as 8, correctly.
  - **Fixed a real gap**: the resend cooldown (`resendCooldown`) was only ever started by a manual resend tap, never by the initial code send itself, so "Resend code" was immediately tappable the instant a user landed on this screen — the export shows the cooldown already counting down (`Resend in 0:24`) on arrival. Now seeded to 30s at the same point `isVerifying` is set true after a successful signup.
- A5 VerifyScreen (error state): code path confirmed present (denied-code error banner logic exists in `AuthScreen.tsx`), not independently screenshotted — triggering it requires a failing `verifyOtp` call, which the QA harness's fake client doesn't yet simulate. Low risk (styling reuses the same already-verified banner pattern used elsewhere in this file), but genuinely unverified by render.
- Forgot Password entry form + forgot-password OTP screen: rendered via the real `pendingResetEmail` prop. No dedicated mockup exists for the plain email-entry step (confirmed via grep — only A4/A5's anatomy was ever meant to be reused here, per this project's own earlier work), and the OTP step matches that same A4 anatomy correctly. **Fixed the identical resend-cooldown gap** here too (`forgotResendCooldown` now seeded to 30s right after `resetPasswordForEmail` succeeds, matching the signup-side fix).
- A2 CountrySelectScreen: re-confirmed matching (already verified in an earlier pass this session).

QA harness additions made to enable this batch (routes only, no behavior change to real app code): `auth-verify-otp` and `auth-forgot-otp` harness routes using the AuthScreen's own existing `pendingVerificationEmail`/`pendingResetEmail` props — both are real props already used by production deep-link flows, not new test-only code paths.

### Batch 2 — SERVICES + BOOKING — ✅ DONE (real screenshot comparison against SV1-SV5/PB1/PMG1/PV3)
- SV1 ServicesHomeScreen: re-confirmed MATCH (category grid, provider carousel).
- SV2 ServiceCategoryScreen: rendered with real fixture data (previously showed a false "0 providers" empty state because the harness's `service_provider_categories` fixture table was empty, not because of a UI bug) — now shows the real provider card matching the export's layout.
- SV3 ServiceProviderProfileScreen: rendered and compared against the export in detail.
  - **Fixed a real bug**: prices everywhere on this screen (starting price, each service's price, cart subtotal, Book & Pay button) showed a bare ISO code ("NGN 85,000") instead of the real currency symbol the export uses everywhere ("₦85,000"). Added a shared `formatServiceAmount()` helper (`src/lib/currencies.ts`) and used it here.
  - **Fixed a real gap**: the export's "No reviews yet for this provider — ratings only appear once real bookings are reviewed" honest-empty banner was completely missing, and worse, this screen never even fetched the real `service_provider_ratings` aggregate that exists for exactly this purpose (`withProviderRatings`, already used by the provider list view but never wired into this profile view). Wired it in: shows the real ★ rating/review count when reviews exist, the honest empty-state banner when they don't — never fabricated.
  - Confirmed real structural deviation, left as-is (not fabricated, not fixed): the export organizes content into Services/About/Reviews tabs; the real screen shows it all in one continuous scroll with a richer multi-service booking cart (checkboxes, quantity, scheduling, wallet/Paystack toggle) that the export's simpler single-tap "Book this provider" doesn't have. This is real, working, more capable functionality — restructuring it into three tabs would be presentation-only churn on business logic that works, not a "fix."
- SV4 BookingSheet: confirmed the export's booking flow (service selection → schedule → pay) lives inline in SV3 above, not a separate sheet — same functional coverage via a different, real interaction pattern.
- SV5 ServiceBookingsScreen / PB1 provider bookings: rendered both customer and provider modes.
  - **Fixed the same currency-symbol bug** here (subtotal, fee, item lines, total).
  - **Fixed a real copy bug**: a refunded booking showed "Total due" (customer) / "You will earn" (provider) instead of "Refunded" — fixed to show "Refunded" whenever `paymentStatus === 'refunded'`, regardless of viewer mode.
- PMG1 ServiceProviderSetupScreen, PV3 ServiceProviderVerificationScreen: re-confirmed MATCH.
- `ServiceProviderCard.tsx` (used on Home's carousel and the category list): same currency-symbol bug found and fixed here too, since it's shared across B1/SV1/SV2.

QA harness additions: `provider-bookings` route (provider-mode `ServiceBookingsScreen`), plus real fixture rows for `provider_services`, `service_provider_categories`, `service_provider_ratings`, and `service_bookings` (with embedded `service_providers`/`service_booking_items`) — needed to actually see these screens' populated states instead of their (also-correct) empty states.

### Batch 3 — CHAT + SEARCH — ✅ DONE (real screenshot comparison against CH1/CH2/PS1)
- CH1 ExploreScreen (Chats tab): rendered with realistic fixture conversations (one unread, one read, real role badges) and compared against the export in detail.
  - **Fixed a real layout mismatch**: the export puts each conversation row's role badge (Organizer/Provider) inline with the MESSAGE PREVIEW (second line), not the contact's name (first line). The real code had it on the name line. Moved it to match.
  - **Fixed a real overflow bug**: the "Service Providers" filter chip label was too long for its pill in the horizontal scroll row (visibly truncating/pushing the row wider than needed). Shortened to "Providers", matching the export's actual label.
  - Online-status green dot code is present and pixel-identical to the export's spec (position, size, color, border) — confirmed present in source and by DOM inspection; not independently confirmed by screenshot pixel color due to a fixture timing artifact (static `Date.now()` captured once at fixture-module load goes stale past the 60s "online" threshold by the time a screenshot is taken later in a long session).
- CH2 Message Requests overlay: rendered and functionally correct (real accept/decline buttons, real "wants to message you" copy, real avatar). **Could not get a clean full-screen screenshot** — verified via DOM inspection that the overlay's `position:fixed` div genuinely computes to the full 390×844 viewport with no ancestor `transform` breaking containment, but the rendered screenshot visually shows only a small card with the Chats screen behind it bleeding through. This reproduced consistently across three separate capture attempts and is most likely a headless-Chromium/no-GPU rendering quirk with this specific multi-stop `radial-gradient` (mixing `rgba()` and a hex color across stops), not a real bug — changing working CSS based on an unconfirmed sandbox-only visual glitch risks making it worse on real devices, so left as-is. Flagging honestly as unverified-by-screenshot rather than claiming a fix.
- PS1 People/Messages search split: rendered with an active search query ("ada"). Confirmed MATCH — real PEOPLE section (avatar, name, verified check, role badge, @username) above a real, honest MESSAGES empty state ("No matching messages") when no conversation matches. Real working feature, not fabricated.
- `InboxScreen.tsx`: confirmed still dead/unreachable (no change made — per this batch's explicit instruction not to revive or substitute it). Left as a known cleanup item, not touched.
- B2/B4 Search screens: not re-touched this batch (out of this batch's explicit scope — Chats + People Search only); still last verified in an earlier part of this session per Batch 1/2 notes.

### Batch 4 — TICKET + SCANNER — ✅ DONE for Scanner verdicts; QRTicket partially verified, one real bug fixed
- **Scanner verdict states — all rendered and confirmed matching the design doc**, using the scanner's own real dev simulator (`isDev` input + Simulate button, exercises the actual `verify_entry_pass` RPC result-mapping code) fed through a QA-only `verify_entry_pass` mock keyed off a magic ticket-id prefix (`VALID-`/`USED-`/`EXPIRED-`/`WRONG-`/`REFUNDED-`/`CANCELLED-`/anything else → not_found). Confirmed real, correctly-colored, correctly-worded verdicts for: CHECKED IN (green), ALREADY CHECKED IN (amber), EXPIRED TICKET (red), WRONG EVENT (red) — all match the design doc's full-screen color-verdict spec. REFUNDED/CANCELLED/INVALID share the same verified code path (`deniedInfo()` switch in `ticketValidation.ts`) and are confirmed correct by source + the same rendering path already proven for the other denial reasons.
  - **Fixed a real bug**: the "Camera Unavailable" fallback had no `outcome === null` guard, so a manual/simulated check-in result rendered ON TOP of the permanent camera-error state instead of replacing it — a real, visible collision on any device with a broken/denied camera that then uses the manual-code fallback (which stays reachable exactly because the camera doesn't work). Fixed to match the `showFrame` pattern already used elsewhere in the same file.
  - Manual code entry sheet: rendered and confirmed matching the design doc's "Enter code manually" anatomy (explanation text, formatted input, Check In CTA).
- **QRTicket (D2)**: "Paid by" provenance line confirmed rendering correctly with real data (only shows when payer ≠ holder, as designed). The light QR backing fixed in an earlier pass is confirmed still in place. **Found and partially fixed a real QA harness gap, not a product bug**: `qa-harness/vite.harness.config.ts` never included the `@tailwindcss/vite` plugin the real `vite.config.ts` uses, so any component built with Tailwind utility classes instead of inline styles (QRTicket.tsx among them) rendered with every one of those classes as a complete no-op in this harness -- severe apparent layout collapse. Added the plugin; it measurably helped (colors/spacing improved) but QRTicket's harness render still shows overlapping header/footer content that contradicts the ALREADY-CONFIRMED real behavior (the user's own phone screenshot earlier in this project, via the real Vercel Preview, showed this exact screen laid out correctly). Treating this as a residual harness-environment artifact (likely incomplete Tailwind JIT class-scanning in this isolated single-entry build), not a real defect — did not modify QRTicket.tsx itself, since there is direct contradicting evidence it already works correctly in production.
- Ticket transfer: confirmed functional (real modal, real RPC) in an earlier pass; no dedicated export screen exists for it beyond an IA label (confirmed earlier this project).
- **Follow-up gap FIXED (post-Batch-4, pre-Batch-5 focused fix)**: `App.tsx`'s `fetchUserTickets` no longer filters with `.eq('status', 'active')` — cancelled/refunded tickets are now fetched and mapped with their real `status`/`payment_status` fields (`PurchasedTicket.status`/`paymentStatus`, `src/app/components/types.ts`). An earlier investigation (requested explicitly before touching any code) confirmed the backend already fully supports this: `select_tickets` RLS has no status filter, `finalize_ticket_refund` already sets `payment_status = 'refunded'` correctly, and `TicketRefundScreen.tsx` already existed with real refund amount/reason/timestamp — so this was a pure frontend navigation/wiring gap, not a missing backend capability. Implemented and **rendered/verified** via the QA harness (`?screen=my-tickets`, two fixture tickets: one active/future-dated, one `status: 'cancelled'`/`paymentStatus: 'refunded'` with the same future event date):
  - `MyTicketsScreen.tsx`: a cancelled/refunded ticket is now always placed in **Past**, never **Upcoming**, regardless of the event's date (`isCancelledOrRefunded()` check ahead of the date comparison in both filters). Confirmed: Upcoming showed 1 (the active ticket), Past showed 1 (the cancelled ticket), correct tab counts.
  - Added a real status badge (`ticketStatusBadge()`) replacing the "View QR" pill on a cancelled/refunded card — confirmed rendering "CANCELLED" (grey) for `status === 'cancelled'`; also handles `paymentStatus === 'refunded'` (red "REFUNDED") and `'refund_pending'` (amber "REFUND PENDING") for tickets that are refunded without a status change.
  - The Transfer Ticket button (already gated to the Upcoming tab) no longer appears on cancelled/refunded cards, since they're now routed to Past.
  - `App.tsx`'s `onViewTicket` passed to `MyTicketsScreen` now checks `ticket.status === 'cancelled' || ticket.paymentStatus === 'refunded' || ticket.paymentStatus === 'refund_pending'` and routes to the existing `ticket-refund` screen (`setRefundTicketId` + `navigateTo('ticket-refund')`) instead of `payment-success`/QR — reusing the exact mechanism a refund notification tap already used. An active ticket's tap-through is unchanged (still opens the QR view via `payment-success`).
  - Typecheck (`tsc --noEmit`) clean; full suite still green at 468/468 (47 files) after these changes.
  - **Not independently re-verified in this pass** (unchanged by this fix, so not re-tested): `TicketRefundScreen.tsx`'s own rendering of real refund amount/reason/timestamp (already confirmed matching mockup S6 earlier), and the live-app tap-through from `MyTicketsScreen` into `TicketRefundScreen` end-to-end inside the full `App.tsx` render tree (verified by reading the exact conditional + state wiring, not by mounting the full app — `TicketRefundScreen` already fetches its own ticket by id independent of what triggered the navigation, so this reuses an already-proven path rather than introducing a new one).

- **My Tickets VISUAL redesign (separate follow-up, done after the functional fix above)**: the functional fix above was mistakenly reported without first confirming `MyTicketsScreen.tsx`'s actual visual match against the export. A direct comparison against the real exported mockup (`stripped.html`, section labeled "D1 · TicketsScreen · upcoming") found three real, concrete gaps and fixed them:
  1. **Header** — was a centered 20px/700 title with a 36px spacer + circular refresh icon; the export shows a left-aligned 28px/800 title with a 42px circular icon button on the right (`rgba(255,255,255,.07)` fill, `rgba(255,255,255,.14)` border). Rebuilt to match.
  2. **Tabs** — was an animated purple-gradient sliding pill indicator with numeric count badges on all three tabs; the export uses a flat selected-tab background (`rgba(255,255,255,.14)`, no gradient/animation) with plain labels and only a small dot on Transfers. Rebuilt to match; the dot now shows only when a transfer is actually pending (real data, not a decorative always-on dot).
  3. **Ticket card footer** — was a single conditional full-width "Transfer Ticket" button below a dashed-divider price row and a separate boxed "Ticket Reference No." row; the export shows two side-by-side buttons directly under the event details — a glass "Transfer" button and a filled `#8E5CF7` "Show QR" button — with no price or reference number depicted at all. Rebuilt the two-button row to match visually; price and reference number were **kept** (compacted into one quiet line above the buttons) rather than removed, since the export is a placeholder mock and dropping real ticket data to chase pixel-parity would violate the "never remove real functional data" rule — this is a deliberate, documented deviation, not an oversight.
  - The export contains **no Past-tab, Transfers-tab, or refunded/cancelled-ticket visual state** — only the Upcoming state (D1) is shown. The existing CANCELLED/REFUNDED badge treatment (from the functional fix above) was left as-is rather than inventing a "designed" terminal-state visual that doesn't exist in the source export.
  - **Rendered and verified** via the QA harness after the rebuild: Upcoming tab (active ticket, new header/tabs/two-button footer, real price+reference line intact), Past tab (cancelled ticket, CANCELLED badge, no action buttons since there's no valid QR), Transfers tab (new header/tab styling carries through, empty-state unaffected).
  - Typecheck clean; full suite still 468/468 (47 files) after the redesign.
  - **Not re-verified**: the QR ticket screen (`QRTicket.tsx`) and `TicketRefundScreen.tsx` themselves were not touched or re-rendered in this pass — only `MyTicketsScreen.tsx`'s own presentation changed.

### Batch 5 — DESKTOP + TABLET
- Only spot-checked: `settings` and `organizer-dashboard` at 1440×900 (both looked correct, grid layouts triggered properly). ManageEventsScreen desktop (DT1) was reviewed earlier and deliberately left as a card grid instead of a literal data table, per explicit user sign-off. Nothing else has been rendered at 834×1112 (tablet) or 1440×900 (desktop) this session. EventDetailsScreen documents a "media-left/sticky-purchase-panel-right" desktop split in comments — never rendered/confirmed.

## Known code-level issues to fix regardless of which batch picks them up
- `CreateEventScreen.tsx` has 4 steps; the newer design doc specifies 6 (details → cover image → schedule/location → tickets → settings → preview/publish). This is a real, large restructuring of a ~2000-line revenue-critical file — do not rush it; read the whole file first.
- `InboxScreen.tsx` dead-code question (see Batch 3 above).

## Environment constraint (applies to every future session)
This sandbox has no live Supabase credentials — the real `App.tsx` cannot boot end-to-end (`supabaseUrl is required` at import time). All rendering must go through `qa-harness/` (`npx vite --config qa-harness/vite.harness.config.ts --port 5199`, then `node qa-harness/screenshot.mjs <screen-key> <width> <height> <out.png>`), which mounts the real, unmodified screen components with a fixture Supabase shim (`qa-harness/fakeSupabase.ts`). Screens/states needing a real network call the shim doesn't cover will need either a new fixture row or an honest note that they're unverifiable here.
