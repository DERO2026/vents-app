# QA-ONLY fake, non-functional values -- never real credentials. Exists
# only so createClient() doesn't throw at import time; the harness's own
# component fixture data is what actually renders (data still comes through
# supabase.from(...) calls which will fail against this fake host -- that's
# fine, it just means components show their own loading/error/empty states,
# which is itself something worth visually checking).
VITE_SUPABASE_URL=https://qa-harness-fake.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJyb2xlIjoiYW5vbiJ9.fake_qa_harness_signature_not_real
VITE_PAYSTACK_PUBLIC_KEY=pk_test_qa_harness_fake
